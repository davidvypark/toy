---
phase: 15-checkout-purchase-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TOY/Features/Monetization/PurchaseService.swift
  - TOY/TOYShared/Sources/TOYShared/Services/CardService.swift
autonomous: true
user_setup:
  - service: supabase
    why: "Record purchase transaction IDs for audit trail (PURCH-01)"
    dashboard_config:
      - task: "Add purchase_transaction_id column to cards table"
        location: "Supabase Dashboard -> SQL Editor"
        sql: "ALTER TABLE cards ADD COLUMN IF NOT EXISTS purchase_transaction_id TEXT;"

must_haves:
  truths:
    - "PurchaseService exposes transaction identifier from RevenueCat purchase"
    - "PurchaseService detects and throws on user cancellation instead of proceeding"
    - "CardService can atomically write maxParticipants and transactionId to a card record"
  artifacts:
    - path: "TOY/Features/Monetization/PurchaseService.swift"
      provides: "purchaseWithTransaction(package:) method returning (CustomerInfo, String?)"
      contains: "purchaseWithTransaction"
    - path: "TOY/Features/Monetization/PurchaseService.swift"
      provides: "PurchaseError.purchaseCancelled case"
      contains: "purchaseCancelled"
    - path: "TOY/TOYShared/Sources/TOYShared/Services/CardService.swift"
      provides: "recordTierPurchase(cardId:maxParticipants:transactionId:) method"
      contains: "recordTierPurchase"
  key_links:
    - from: "PurchaseService.purchaseWithTransaction"
      to: "Purchases.shared.purchase(package:)"
      via: "RevenueCat SDK call extracting StoreTransaction.transactionIdentifier"
      pattern: "transaction.*transactionIdentifier"
    - from: "CardService.recordTierPurchase"
      to: "Supabase cards table"
      via: "update with max_participants and purchase_transaction_id columns"
      pattern: "purchase_transaction_id.*transactionId"
---

<objective>
Add purchase infrastructure methods to PurchaseService and CardService so that the checkout flow (Plan 02) can execute a purchase, capture the transaction ID, and record the tier upgrade to Supabase atomically.

Purpose: Plan 02's CheckoutSheet needs to (a) execute a purchase and detect cancellation, (b) get the transaction ID for audit, and (c) record both maxParticipants and transactionId to the card. These service methods must exist first.

Output: Modified PurchaseService.swift with `purchaseWithTransaction(package:)` and `PurchaseError.purchaseCancelled`. Modified CardService.swift with `recordTierPurchase(cardId:maxParticipants:transactionId:)`.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-checkout-purchase-flow/15-RESEARCH.md

@TOY/Features/Monetization/PurchaseService.swift
@TOY/TOYShared/Sources/TOYShared/Services/CardService.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add purchaseWithTransaction method and cancellation error to PurchaseService</name>
  <files>TOY/Features/Monetization/PurchaseService.swift</files>
  <action>
  1. Add `case purchaseCancelled` to `PurchaseError` enum with `errorDescription` returning "Purchase was cancelled".

  2. Add a NEW method `purchaseWithTransaction(package:)` to `PurchaseService` that:
     - Calls `Purchases.shared.purchase(package: package)` capturing ALL three tuple values: `(transaction, customerInfo, userCancelled)`
     - If `userCancelled` is true, throws `PurchaseError.purchaseCancelled`
     - Returns `(customerInfo: CustomerInfo, transactionId: String?)` where `transactionId` is `transaction?.transactionIdentifier`
     - Catches `PurchaseError` and re-throws (to preserve `.purchaseCancelled`), catches other errors as `PurchaseError.purchaseFailed`

  3. Do NOT modify the existing `purchase(package:)` method. It must remain as-is for backward compatibility with UpgradeViewModel (removed in Phase 16).

  The method signature should be:
  ```swift
  public func purchaseWithTransaction(package: Package) async throws -> (customerInfo: CustomerInfo, transactionId: String?) {
  ```
  </action>
  <verify>Build the project with Cmd+B (or `xcodebuild build`). Confirm PurchaseService.swift compiles with both `purchase(package:)` and `purchaseWithTransaction(package:)` methods. Confirm `PurchaseError.purchaseCancelled` is a valid case.</verify>
  <done>PurchaseService has a `purchaseWithTransaction(package:)` method that returns the transaction identifier and handles user cancellation. The old `purchase(package:)` method is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add recordTierPurchase method to CardService</name>
  <files>TOY/TOYShared/Sources/TOYShared/Services/CardService.swift</files>
  <action>
  Add a new public method `recordTierPurchase` to `CardService` that atomically writes both `max_participants` and `purchase_transaction_id` to a card record in Supabase.

  Method signature:
  ```swift
  public func recordTierPurchase(
      cardId: UUID,
      maxParticipants: Int,
      transactionId: String
  ) async throws {
  ```

  Implementation:
  - Call `supabase.from("cards").update(["max_participants": maxParticipants, "purchase_transaction_id": transactionId]).eq("id", value: cardId).execute()`
  - Follow the exact same error handling pattern as `updateCardMaxParticipants`: `catch { if Task.isCancelled { throw CancellationError() }; throw CardError.updateFailed(error.localizedDescription) }`
  - Add a `#if DEBUG print` on success matching the existing pattern

  Place this method directly AFTER the existing `updateCardMaxParticipants` method (around line 225) in the same MARK section.

  Do NOT modify the existing `updateCardMaxParticipants` method. It stays for backward compatibility.

  NOTE: This method requires the `purchase_transaction_id TEXT` column to exist in the Supabase `cards` table. The user_setup section of this plan covers that migration.
  </action>
  <verify>Build the project with Cmd+B. Confirm CardService.swift compiles. Verify that `recordTierPurchase(cardId:maxParticipants:transactionId:)` is accessible from outside the module (it's `public` on an `actor`).</verify>
  <done>CardService has a `recordTierPurchase` method that writes both maxParticipants and transactionId to the card record in a single Supabase update call. The existing `updateCardMaxParticipants` method is unchanged.</done>
</task>

</tasks>

<verification>
1. Project builds without errors
2. PurchaseService.swift has both `purchase(package:)` (unchanged) and `purchaseWithTransaction(package:)` (new)
3. PurchaseError has `.purchaseCancelled` case
4. CardService.swift has both `updateCardMaxParticipants` (unchanged) and `recordTierPurchase` (new)
5. No existing tests broken (if any exist)
</verification>

<success_criteria>
- PurchaseService exposes transaction ID from RevenueCat purchase response
- PurchaseService explicitly handles user cancellation (does not silently proceed)
- CardService can write maxParticipants + transactionId to Supabase in one update
- All existing methods unchanged for backward compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/15-checkout-purchase-flow/15-01-SUMMARY.md`
</output>

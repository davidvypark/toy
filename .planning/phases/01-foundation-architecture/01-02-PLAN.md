---
phase: 01-foundation-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/001_initial_schema.sql
  - supabase/seed.sql
  - .env.example
autonomous: false

user_setup:
  - service: supabase
    why: "Backend database and authentication"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
    dashboard_config:
      - task: "Create new Supabase project"
        location: "https://supabase.com/dashboard -> New Project"
      - task: "Run migration SQL in SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> New Query"

must_haves:
  truths:
    - "Database schema supports cards, clips, and participants"
    - "Row Level Security policies protect user data"
    - "Tables have proper foreign key relationships"
  artifacts:
    - path: "supabase/migrations/001_initial_schema.sql"
      provides: "Database schema with RLS policies"
      contains: "CREATE TABLE"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "SUPABASE_URL"
  key_links:
    - from: "clips table"
      to: "cards table"
      via: "card_id foreign key"
      pattern: "REFERENCES cards"
    - from: "participants table"
      to: "cards table"
      via: "card_id foreign key"
      pattern: "REFERENCES cards"
---

<objective>
Design and create the Supabase database schema for TOY, including tables for users, cards, clips, and participants with proper Row Level Security.

Purpose: Establishes the data layer that all features will build upon. The schema design affects every future phase.

Output: SQL migration files ready to execute in Supabase, plus environment variable template.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Database Migration SQL</name>
  <files>
    supabase/migrations/001_initial_schema.sql
  </files>
  <action>
Create supabase/ directory at project root with migrations/ subdirectory.

Create 001_initial_schema.sql with the following schema:

**profiles table** (extends Supabase auth.users):
- id: UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE
- email: TEXT
- display_name: TEXT
- avatar_url: TEXT
- created_at: TIMESTAMPTZ DEFAULT now()
- updated_at: TIMESTAMPTZ DEFAULT now()

**cards table**:
- id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
- host_id: UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE
- title: TEXT NOT NULL (e.g., "Happy Birthday Sarah!")
- recipient_name: TEXT NOT NULL
- occasion: TEXT (birthday, wedding, get_well, congratulations, holiday, other)
- status: TEXT DEFAULT 'draft' CHECK (status IN ('draft', 'collecting', 'stitching', 'published'))
- published_at: TIMESTAMPTZ
- video_url: TEXT (final stitched video URL)
- share_token: TEXT UNIQUE (for recipient viewing)
- max_participants: INTEGER DEFAULT 8
- created_at: TIMESTAMPTZ DEFAULT now()
- updated_at: TIMESTAMPTZ DEFAULT now()

**clips table**:
- id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
- card_id: UUID NOT NULL REFERENCES cards(id) ON DELETE CASCADE
- participant_id: UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE
- video_url: TEXT NOT NULL (Supabase storage URL)
- duration_seconds: DECIMAL(4,2) CHECK (duration_seconds <= 7.0)
- order_position: INTEGER (for montage ordering, host clip = 0)
- status: TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'uploaded', 'approved', 'rejected'))
- uploaded_at: TIMESTAMPTZ
- created_at: TIMESTAMPTZ DEFAULT now()

**participants table** (invitations):
- id: UUID PRIMARY KEY DEFAULT gen_random_uuid()
- card_id: UUID NOT NULL REFERENCES cards(id) ON DELETE CASCADE
- user_id: UUID REFERENCES profiles(id) ON DELETE SET NULL (null until they record)
- invite_token: TEXT UNIQUE NOT NULL (for deep link)
- email: TEXT (optional - if host invites by email)
- status: TEXT DEFAULT 'invited' CHECK (status IN ('invited', 'viewed', 'recording', 'submitted'))
- invited_at: TIMESTAMPTZ DEFAULT now()
- submitted_at: TIMESTAMPTZ

Add Row Level Security policies:
- profiles: Users can read their own profile, update their own profile
- cards: Hosts can CRUD their own cards, participants can read cards they're invited to
- clips: Participants can CRUD their own clips, hosts can read/delete clips on their cards
- participants: Hosts can CRUD participants on their cards, users can read their own invitations

Add indexes for common queries:
- cards(host_id)
- cards(share_token)
- clips(card_id)
- clips(participant_id)
- participants(card_id)
- participants(invite_token)
- participants(user_id)

Add trigger for updated_at timestamp on profiles and cards.
  </action>
  <verify>
Syntax check: The SQL should be valid PostgreSQL syntax.
Run: `cat supabase/migrations/001_initial_schema.sql | head -100` to verify structure
  </verify>
  <done>
Migration SQL file exists with all tables, RLS policies, and indexes defined
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Seed Data and Environment Template</name>
  <files>
    supabase/seed.sql
    .env.example
  </files>
  <action>
Create supabase/seed.sql with sample data for development:
- Comment explaining this is for local development only
- Sample card data (commented out, requires valid user IDs)

Create .env.example at project root:
```
# Supabase Configuration
# Get these from: Supabase Dashboard -> Project Settings -> API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# App Configuration
APP_BUNDLE_ID=com.yourcompany.TOY
```

This template will be used to create the actual .env file (which is gitignored).
  </action>
  <verify>
Files exist: `ls -la supabase/ .env.example`
  </verify>
  <done>
Seed file and environment template created
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-to-do>Create Supabase project and run migration</what-to-do>
  <instructions>
1. Go to https://supabase.com/dashboard
2. Click "New Project" and create a project named "toy-dev" (or similar)
3. Wait for project to be ready (~2 minutes)
4. Go to SQL Editor (left sidebar)
5. Click "New Query"
6. Copy the contents of `supabase/migrations/001_initial_schema.sql`
7. Paste into the query editor and click "Run"
8. Verify tables were created: Go to Table Editor and confirm you see: profiles, cards, clips, participants
9. Copy your API credentials:
   - Go to Project Settings -> API
   - Copy "Project URL" (SUPABASE_URL)
   - Copy "anon public" key (SUPABASE_ANON_KEY)
10. Create `.env` file from `.env.example` and fill in the values
  </instructions>
  <resume-signal>Type "supabase ready" once the database is set up and .env file is created</resume-signal>
</task>

</tasks>

<verification>
1. supabase/migrations/001_initial_schema.sql exists with complete schema
2. .env.example exists with template variables
3. User has created Supabase project and run migration
4. .env file exists with actual credentials (gitignored)
</verification>

<success_criteria>
- Database schema SQL is complete and valid
- All four tables defined: profiles, cards, clips, participants
- RLS policies protect data appropriately
- Environment template ready for credential storage
- Supabase project created and migration executed (after user action)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-architecture/01-02-SUMMARY.md`
</output>

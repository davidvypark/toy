---
phase: 01-foundation-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TOYShared/Package.swift
  - TOYShared/Sources/TOYShared/TOYShared.swift
  - TOYShared/Sources/TOYShared/Models/.gitkeep
  - TOYShared/Sources/TOYShared/Services/.gitkeep
  - TOYShared/Sources/TOYShared/Theme/.gitkeep
  - TOYShared/Sources/TOYShared/Components/.gitkeep
  - TOYShared/Tests/TOYSharedTests/TOYSharedTests.swift
autonomous: true

must_haves:
  truths:
    - "TOYShared package compiles without errors"
    - "TOYShared can be imported in main app target"
    - "Package includes supabase-swift dependency"
  artifacts:
    - path: "TOYShared/Package.swift"
      provides: "Swift package definition with Supabase dependency"
      contains: "supabase-swift"
    - path: "TOYShared/Sources/TOYShared/TOYShared.swift"
      provides: "Package entry point"
      min_lines: 5
  key_links:
    - from: "TOY.xcodeproj"
      to: "TOYShared"
      via: "Local package dependency"
      pattern: "TOYShared"
---

<objective>
Create the TOYShared Swift Package as the foundation for code sharing between the main app and future App Clip target.

Purpose: Establishes the shared code architecture that enables code reuse and keeps the App Clip binary size manageable by sharing components.

Output: A local Swift package at TOYShared/ with proper structure, supabase-swift dependency, and integration with the main Xcode project.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TOYShared Package Structure</name>
  <files>
    TOYShared/Package.swift
    TOYShared/Sources/TOYShared/TOYShared.swift
    TOYShared/Tests/TOYSharedTests/TOYSharedTests.swift
  </files>
  <action>
Create the TOYShared directory at the project root level (same level as TOY.xcodeproj).

Create Package.swift with:
- Package name: "TOYShared"
- Platforms: [.iOS(.v17)]
- Products: library named "TOYShared" with target "TOYShared"
- Dependencies: supabase-swift from "https://github.com/supabase/supabase-swift.git", from: "2.0.0"
- Targets:
  - .target(name: "TOYShared", dependencies: [.product(name: "Supabase", package: "supabase-swift")])
  - .testTarget(name: "TOYSharedTests", dependencies: ["TOYShared"])

Create TOYShared.swift as the package entry point with:
- A public struct TOYShared with a static property `version = "1.0.0"`
- A public init() for the struct

Create TOYSharedTests.swift with:
- A simple test that verifies TOYShared.version equals "1.0.0"

Create directory structure placeholders:
- TOYShared/Sources/TOYShared/Models/ (add .gitkeep)
- TOYShared/Sources/TOYShared/Services/ (add .gitkeep)
- TOYShared/Sources/TOYShared/Theme/ (add .gitkeep)
- TOYShared/Sources/TOYShared/Components/ (add .gitkeep)
  </action>
  <verify>
Run: `cd /Users/davidpark/Development/code/TOY/TOYShared && swift build`
Expected: Build succeeds, package resolves supabase-swift dependency
  </verify>
  <done>
TOYShared package builds successfully with supabase-swift dependency resolved
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Package with Xcode Project</name>
  <files>
    TOY.xcodeproj/project.pbxproj
    TOY/TOYApp.swift
  </files>
  <action>
Add TOYShared as a local package dependency to the TOY Xcode project.

Use xcodebuild or direct pbxproj manipulation:
1. Add the local package reference to the project
2. Add TOYShared framework to the TOY app target's "Frameworks, Libraries, and Embedded Content"

Then update TOYApp.swift to import TOYShared and print the version on launch (temporary verification):
```swift
import SwiftUI
import TOYShared

@main
struct TOYApp: App {
    init() {
        print("TOYShared version: \(TOYShared.version)")
    }

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

Note: The cleanest way to add a local package is via Xcode's "Add Package Dependencies" but since we're automating, we can use `xcodebuild` to verify the integration works after manual pbxproj edits, OR create a simple test by building.

Alternative approach if pbxproj editing is complex: Create clear instructions for the package addition and verify the package builds independently. The Xcode integration can be verified in the final checkpoint plan.
  </action>
  <verify>
Run: `cd /Users/davidpark/Development/code/TOY && xcodebuild -project TOY.xcodeproj -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`
Expected: Build succeeds with TOYShared imported
  </verify>
  <done>
Main app target builds successfully with TOYShared imported and version prints on launch
  </done>
</task>

</tasks>

<verification>
1. TOYShared/Package.swift exists and contains supabase-swift dependency
2. `swift build` in TOYShared directory succeeds
3. Directory structure exists: Models/, Services/, Theme/, Components/
4. TOY app builds with TOYShared imported
</verification>

<success_criteria>
- TOYShared Swift package compiles independently
- supabase-swift dependency resolves correctly
- Main app can import and use TOYShared
- Package structure ready for subsequent plans to add Models, Services, Theme, Components
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-architecture/01-01-SUMMARY.md`
</output>

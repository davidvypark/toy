---
phase: 16-cleanup-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TOY/Features/Monetization/CardUpgradeView.swift      # DELETE
  - TOY/Features/Monetization/UpgradeViewModel.swift      # DELETE
  - TOY/Features/Monetization/PurchaseService.swift        # Remove 3 dead methods + update doc comment
  - TOY/TOYShared/Sources/TOYShared/Services/CardService.swift  # Remove 1 dead method
  - TOY/Features/CardManagement/CardDetailView.swift       # Remove dead state + sheet
autonomous: true

must_haves:
  truths:
    - "No reference to CardUpgradeView, UpgradeViewModel, showUpgradeSheet, isCardUpgraded, or updateCardMaxParticipants exists in any Swift file"
    - "The project builds without errors after all removals"
    - "PurchaseService retains purchaseWithTransaction, fetchTierPackages, restorePurchases, getCustomerInfo, hasEntitlement"
    - "CardService retains recordTierPurchase"
  artifacts:
    - path: "TOY/Features/Monetization/CardUpgradeView.swift"
      provides: "DELETED - must not exist"
    - path: "TOY/Features/Monetization/UpgradeViewModel.swift"
      provides: "DELETED - must not exist"
    - path: "TOY/Features/Monetization/PurchaseService.swift"
      provides: "Cleaned PurchaseService with only new tier methods"
      contains: "purchaseWithTransaction"
    - path: "TOY/TOYShared/Sources/TOYShared/Services/CardService.swift"
      provides: "Cleaned CardService with only recordTierPurchase"
      contains: "recordTierPurchase"
    - path: "TOY/Features/CardManagement/CardDetailView.swift"
      provides: "CardDetailView with no old upgrade references"
  key_links:
    - from: "TOY/Features/Monetization/CheckoutSheet.swift"
      to: "PurchaseService.purchaseWithTransaction"
      via: "purchase call in checkout flow"
      pattern: "purchaseWithTransaction"
    - from: "TOY/Features/Publishing/MontagePreviewView.swift"
      to: "CheckoutSheet"
      via: "sheet presentation on needsUpgrade"
      pattern: "showCheckout"
---

<objective>
Remove all old binary upgrade system code -- CardUpgradeView, UpgradeViewModel, deprecated PurchaseService methods, dead CardService method, and dead CardDetailView references -- leaving only the new tier-based monetization system from Phases 13-15.

Purpose: Complete CLEAN-01 requirement. The old system creates confusion and maintenance burden alongside the new tier system.
Output: Clean codebase with zero references to old upgrade system, project builds cleanly.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cleanup-verification/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old upgrade files and remove dead CardDetailView references</name>
  <files>
    TOY/Features/Monetization/CardUpgradeView.swift
    TOY/Features/Monetization/UpgradeViewModel.swift
    TOY/Features/CardManagement/CardDetailView.swift
  </files>
  <action>
    1. Delete `TOY/Features/Monetization/CardUpgradeView.swift` entirely (178 lines, old binary upgrade UI).
    2. Delete `TOY/Features/Monetization/UpgradeViewModel.swift` entirely (145 lines, old binary upgrade VM).
    3. In `TOY/Features/CardManagement/CardDetailView.swift`:
       - Remove `@State private var showUpgradeSheet = false` (line 19)
       - Remove the `.sheet(isPresented: $showUpgradeSheet) { CardUpgradeView(...) }` block (lines 231-236)
       - Do NOT remove any other state or sheets -- `showTierSelection`, `showCheckout`, etc. are the NEW system

    IMPORTANT: Do NOT remove any files or references related to the new tier system:
    - CheckoutSheet.swift STAYS
    - TierIndicatorView.swift STAYS
    - TierSelectionSheet.swift STAYS
    - CardDetailViewModel.swift STAYS (needsUpgradeToPublish is new system)
  </action>
  <verify>
    Run: `grep -rn "CardUpgradeView\|UpgradeViewModel\|showUpgradeSheet" TOY/ --include="*.swift"` -- must return zero matches.
    Confirm CardUpgradeView.swift and UpgradeViewModel.swift no longer exist on disk.
  </verify>
  <done>Both old files deleted. CardDetailView has no references to showUpgradeSheet or CardUpgradeView.</done>
</task>

<task type="auto">
  <name>Task 2: Remove deprecated methods from PurchaseService and CardService</name>
  <files>
    TOY/Features/Monetization/PurchaseService.swift
    TOY/TOYShared/Sources/TOYShared/Services/CardService.swift
  </files>
  <action>
    In `TOY/Features/Monetization/PurchaseService.swift`:
    1. Remove `fetchOfferings() -> Offerings` method (lines 56-58). Only caller was UpgradeViewModel (now deleted).
    2. Remove the old `purchase(package:) -> CustomerInfo` method (lines 83-95, including its doc comment starting at line 83). Only caller was UpgradeViewModel. Superseded by `purchaseWithTransaction(package:)`.
    3. Remove `isCardUpgraded(cardId:) -> Bool` method (lines 165-178). Already `@available(*, deprecated)`, zero callers.
    4. Update the doc comment at the top of PurchaseService (around lines 34-40) which references `fetchOfferings()` in its usage example. Replace with `fetchTierPackages()` in the example, or remove the example if it only shows the old flow.

    In `TOY/TOYShared/Sources/TOYShared/Services/CardService.swift`:
    5. Remove `updateCardMaxParticipants(cardId:maxParticipants:)` method (lines 210-225). Only caller was UpgradeViewModel. Superseded by `recordTierPurchase(cardId:maxParticipants:transactionId:)`.

    IMPORTANT: Do NOT remove these methods -- they are the NEW system or general-purpose:
    - `purchaseWithTransaction(package:)` -- NEW purchase method (Phase 15)
    - `fetchTierPackages()` -- NEW tier-specific loader (Phase 13)
    - `restorePurchases()` -- general-purpose, may be needed for App Store review
    - `getCustomerInfo()` -- general-purpose
    - `hasEntitlement(_:)` -- general-purpose
    - `recordTierPurchase(cardId:maxParticipants:transactionId:)` -- NEW recording method (Phase 15)
  </action>
  <verify>
    Run: `grep -rn "isCardUpgraded\|fetchOfferings\|updateCardMaxParticipants" TOY/ --include="*.swift"` -- must return zero matches.
    Run: `grep -rn "func purchase(package:" TOY/ --include="*.swift"` -- must return zero matches (only `purchaseWithTransaction` should remain).
    Run: `grep -rn "purchaseWithTransaction\|fetchTierPackages\|recordTierPurchase\|restorePurchases" TOY/ --include="*.swift"` -- must return matches confirming new system methods still exist.
  </verify>
  <done>PurchaseService has 3 fewer methods (purchase, fetchOfferings, isCardUpgraded). CardService has 1 fewer method (updateCardMaxParticipants). New system methods all intact.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification and final dead code grep</name>
  <files></files>
  <action>
    1. Build the project: `xcodebuild build -project TOY/TOY.xcodeproj -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -20`
       If build fails, fix any remaining references to deleted types/methods.
    2. Run comprehensive dead code grep:
       `grep -rn "CardUpgradeView\|UpgradeViewModel\|showUpgradeSheet\|isCardUpgraded\|fetchOfferings\|updateCardMaxParticipants\|card_upgrade" TOY/ --include="*.swift"`
       Must return zero matches.
    3. Verify new system files still exist:
       - `ls TOY/Features/Monetization/CheckoutSheet.swift`
       - `ls TOY/Features/Monetization/TierIndicatorView.swift`
       - `ls TOY/Features/Monetization/TierSelectionSheet.swift`
       - `ls TOY/Features/Monetization/PurchaseService.swift`
       All must exist.
  </action>
  <verify>
    xcodebuild build succeeds with zero errors.
    Dead code grep returns zero matches.
    All 4 new system files confirmed on disk.
  </verify>
  <done>Project builds cleanly. Zero references to old upgrade system. All new tier system files intact. Criterion 1 (old system completely removed) is satisfied.</done>
</task>

</tasks>

<verification>
1. `CardUpgradeView.swift` and `UpgradeViewModel.swift` do not exist on disk
2. `grep -rn "CardUpgradeView\|UpgradeViewModel\|showUpgradeSheet\|isCardUpgraded\|fetchOfferings\|updateCardMaxParticipants" TOY/ --include="*.swift"` returns zero matches
3. `xcodebuild build` succeeds with no errors
4. New system files (CheckoutSheet, TierIndicatorView, TierSelectionSheet, PurchaseService with purchaseWithTransaction) all present and intact
</verification>

<success_criteria>
- The old CardUpgradeView, UpgradeViewModel, and all deprecated methods are completely removed
- The project compiles cleanly with no references to the old upgrade system
- The new tier system (CheckoutSheet, TierIndicatorView, TierSelectionSheet, purchaseWithTransaction, fetchTierPackages, recordTierPurchase) is fully intact
</success_criteria>

<output>
After completion, create `.planning/phases/16-cleanup-verification/16-01-SUMMARY.md`
</output>

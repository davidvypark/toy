---
phase: 16-cleanup-verification
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "A card with 3 clips publishes for free with no payment interaction"
    - "A card with 12 clips presents the checkout, auto-selects the correct tier, completes purchase, and publishes successfully"
    - "A card with maxParticipants=8 (legacy) and 6 clips publishes for free (grandfathered)"
    - "A card where publish fails after payment can retry publishing without re-purchasing"
  artifacts: []
  key_links:
    - from: "MontagePreviewView"
      to: "card.maxParticipants"
      via: "sortedClips.count > card.maxParticipants comparison"
      pattern: "sortedClips.count > card.maxParticipants"
    - from: "MontagePreviewView"
      to: "CheckoutSheet"
      via: "showCheckout state triggers sheet"
      pattern: "showCheckout"
    - from: "MontagePreviewView"
      to: "publishWithStitching"
      via: "hasPurchased bypass on retry"
      pattern: "hasPurchased"
---

<objective>
Verify the complete tier-based monetization system works correctly across all edge cases: free publish, paid publish, grandfathered cards, and post-payment retry.

Purpose: Confirm success criteria 2-5 with human testing. Code paths have been traced in 16-RESEARCH.md; this plan validates runtime behavior with real StoreKit sandbox, Supabase connectivity, and user interaction.
Output: Verified system behavior across all tier paths. Phase 16 and v1.2 milestone complete.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cleanup-verification/16-RESEARCH.md
@.planning/phases/16-cleanup-verification/16-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete v1.2 seat-based monetization system: tier computation (Phase 13), tier awareness UI (Phase 14), checkout and purchase flow (Phase 15), and old system removal (Phase 16 Plan 01). The new system gates publishing based on clip count vs. card.maxParticipants, presents a multi-tier checkout when needed, records purchases to Supabase, and handles retry after payment failure.
  </what-built>
  <how-to-verify>
    **Prerequisites:** App Store Connect products created (consumable IAPs), RevenueCat offering configured, TOYProducts.storekit added to Xcode scheme, Supabase purchase_transaction_id column added.

    **Test 1 -- Free publish (Criterion 2):**
    1. Create a new card (maxParticipants defaults to 5)
    2. Add 3 clips
    3. Tap Publish
    4. Expected: Montage stitching starts immediately. No checkout sheet appears. Card publishes successfully.

    **Test 2 -- Paid publish (Criterion 3):**
    1. Create a new card (maxParticipants defaults to 5)
    2. Add 12 clips
    3. Tap Publish
    4. Expected: CheckoutSheet appears with Group tier auto-selected (12 <= 25)
    5. Cannot select a tier below clip count
    6. Tap purchase button, complete StoreKit sandbox purchase
    7. Expected: Purchase completes, montage stitching starts, card publishes successfully
    8. Verify in Supabase: card.maxParticipants updated, purchase_transaction_id set

    **Test 3 -- Grandfathered card (Criterion 4):**
    1. In Supabase Dashboard, set a card's maxParticipants to 8
    2. Ensure that card has 6 clips
    3. Tap Publish on that card
    4. Expected: Montage stitching starts immediately. No checkout sheet appears. (6 <= 8 = grandfathered)

    **Test 4 -- Retry after payment (Criterion 5):**
    1. Create a card needing upgrade (e.g., 7 clips, maxParticipants=5)
    2. Tap Publish, complete purchase in CheckoutSheet
    3. Simulate publish failure (e.g., enable airplane mode after payment succeeds, before publish completes)
    4. Disable airplane mode
    5. Tap Publish again
    6. Expected: No checkout sheet appears (hasPurchased=true in session). Publish attempt proceeds directly.

    **Test 5 -- Old system removed (Criterion 1):**
    1. Navigate to Card Detail view
    2. Expected: No "Upgrade for Unlimited" banner anywhere
    3. Only the new TierIndicatorView appears (when card exceeds free limit)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe which tests failed and what happened.</resume-signal>
</task>

</tasks>

<verification>
All 5 success criteria from ROADMAP.md Phase 16 confirmed by human testing:
1. Old system completely removed (verified by Plan 01 grep + Test 5 visual confirmation)
2. Free card publishes without payment (Test 1)
3. Paid card completes checkout and publishes (Test 2)
4. Grandfathered card publishes free (Test 3)
5. Retry after payment skips checkout (Test 4)
</verification>

<success_criteria>
- All 5 human tests pass
- Phase 16 complete
- v1.2 Seat-Based Monetization milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/16-cleanup-verification/16-02-SUMMARY.md`
</output>

---
phase: 03-data-layer-upload
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Video upload completes successfully to Supabase storage"
    - "Upload progress UI displays correctly"
    - "Signed URLs can access uploaded videos"
    - "Deep link parsing works for card URLs"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 3 implementation on a physical device and confirm all success criteria are met.

Purpose: End-to-end verification ensures the upload pipeline and deep link infrastructure work in the real environment, not just in simulation.

Output: Human-verified confirmation that all Phase 3 success criteria pass.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-layer-upload/03-01-SUMMARY.md
@.planning/phases/03-data-layer-upload/03-02-SUMMARY.md
@.planning/phases/03-data-layer-upload/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure Supabase storage bucket is configured</name>
  <files>N/A</files>
  <action>
Verify the clips bucket exists in Supabase and has correct policies:

1. Check if the SQL migration was applied by reviewing 03-01-SUMMARY.md
2. If manual application was needed, provide verification steps:
   - Go to Supabase Dashboard -> Storage
   - Confirm "clips" bucket exists
   - Go to Authentication -> Policies
   - Confirm storage.objects has INSERT, SELECT, UPDATE, DELETE policies for clips bucket

3. If bucket doesn't exist, output the SQL from 002_storage_policies.sql with instructions to run it in Supabase SQL Editor.

This is a pre-flight check before human testing.
  </action>
  <verify>Confirm clips bucket status is known (either verified or instructions provided)</verify>
  <done>Supabase storage bucket status confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Build and prepare for device testing</name>
  <files>N/A</files>
  <action>
Build the app for physical device testing:

1. Run clean build:
   ```bash
   cd /Users/davidpark/Development/code/TOY
   xcodebuild -scheme TOY -destination 'generic/platform=iOS' clean build 2>&1 | tail -30
   ```

2. If build succeeds, provide instructions for device deployment:
   - Open TOY.xcodeproj in Xcode
   - Select physical device as target
   - Build and run (Cmd+R)

3. If build fails, analyze errors and fix before proceeding.
  </action>
  <verify>Build succeeds for iOS device target</verify>
  <done>App builds successfully and is ready for device testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 implementation:
- StorageService with uploadVideo and createSignedURL
- UploadProgressView with uploading/success/failed states
- RecordingViewModel wired to upload on "Use Video"
- Retry logic with exponential backoff
- DeepLinkService for URL parsing
- Associated Domains entitlement
- onOpenURL handler in TOYApp
  </what-built>
  <how-to-verify>
**Test 1: Upload Flow**
1. Launch app on physical device
2. Sign in (or use existing session)
3. Navigate to "Record Video"
4. Record a short clip (hold to record)
5. Tap "Done" to finish recording
6. On preview, tap "Use Video"
7. VERIFY: Upload spinner appears with "Uploading..."
8. VERIFY: After upload completes, checkmark appears with "Uploaded!"
9. Tap "Done" to dismiss

**Test 2: Verify Upload in Supabase**
1. Go to Supabase Dashboard -> Storage -> clips bucket
2. VERIFY: New .mov file exists with recent timestamp
3. Click the file -> Get URL -> Create signed URL
4. VERIFY: Signed URL works (video plays in browser)

**Test 3: Upload Retry (Optional - simulate failure)**
1. If possible, test with airplane mode:
   - Start upload -> Enable airplane mode
   - VERIFY: Error state appears with "Retry" button
   - Disable airplane mode -> Tap "Retry"
   - VERIFY: Upload completes on retry

**Test 4: Deep Link Parsing (Code-level)**
Note: Full Universal Link testing requires AASA file on production domain.
For now, verify the code compiles and onOpenURL is wired:
1. Check Xcode console when app launches
2. If you can test with custom URL scheme (xcrun simctl openurl), verify parsing

**Success Criteria Checklist:**
- [ ] User can submit recorded clip and see upload progress indicator
- [ ] Video files are stored in Supabase storage (clips bucket)
- [ ] Deep link infrastructure is in place (full testing in Phase 4+)
- [ ] Videos accessible via signed URLs (verified in Supabase dashboard)
- [ ] Upload retry works on failure
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification of all Phase 3 success criteria:
1. User can submit recorded clip and see upload progress indicator
2. Video files are stored in Supabase storage (clips bucket)
3. Deep links infrastructure ready (full URL testing requires AASA)
4. Videos are accessible via signed URLs
5. Upload completes reliably with retry on failure
</verification>

<success_criteria>
All 5 Phase 3 success criteria verified on physical device with real Supabase storage
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-layer-upload/03-04-SUMMARY.md`
</output>

---
phase: 09-quick-playback-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TOY/TOYShared/Sources/TOYShared/Recording/UI/RecordingViewModel.swift
autonomous: true

must_haves:
  truths:
    - "Newly recorded clips generate thumbnails at ~1.5s where the person is composed, not mid-setup at 0.5s"
    - "Video audio plays correctly when device ringer/silent switch is set to silent mode"
  artifacts:
    - path: "TOY/TOYShared/Sources/TOYShared/Recording/UI/RecordingViewModel.swift"
      provides: "Thumbnail generation at 1.5s into clip"
      contains: "CMTime(seconds: 1.5"
  key_links:
    - from: "RecordingViewModel.swift"
      to: "AVAssetImageGenerator"
      via: "generateThumbnail() uses 1.5s time"
      pattern: "CMTime\\(seconds:\\s*1\\.5"
---

<objective>
Change thumbnail generation time from 0.5s to 1.5s for newly recorded clips, and verify the existing audio session configuration handles silent mode correctly.

Purpose: At 0.5s into a 7-second clip, the person is typically still adjusting after pressing record. At 1.5s they are composed and making eye contact -- a much better thumbnail. The audio session fix is already implemented but needs verification.
Output: Modified RecordingViewModel.swift with updated thumbnail time constant. Verified audio session in TOYApp.swift.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-quick-playback-wins/09-RESEARCH.md
@TOY/TOYShared/Sources/TOYShared/Recording/UI/RecordingViewModel.swift
@TOY/TOYApp.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change thumbnail generation time to 1.5s (QUAL-01)</name>
  <files>
    TOY/TOYShared/Sources/TOYShared/Recording/UI/RecordingViewModel.swift
  </files>
  <action>
In RecordingViewModel.swift, find the `generateThumbnail(from:)` method. Locate the line (around line 309):
```swift
let time = CMTime(seconds: 0.5, preferredTimescale: 600)
```
Change `0.5` to `1.5`:
```swift
let time = CMTime(seconds: 1.5, preferredTimescale: 600)
```

Do NOT change any other parameters in the thumbnail generation:
- Keep `maximumSize: CGSize(width: 150, height: 200)` -- appropriate for thumbnail resolution
- Keep `appliesPreferredTrackTransform = true` -- ensures correct orientation
- Keep `compressionQuality: 0.7` -- appropriate for thumbnails

This change only affects NEW clips recorded after deployment. Existing clips already have thumbnails stored in Supabase and will not be affected. No backfill migration is needed.
  </action>
  <verify>
Build the project with `xcodebuild build -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5` and confirm no errors. Grep to confirm: `grep -n "CMTime(seconds:" TOY/TOYShared/Sources/TOYShared/Recording/UI/RecordingViewModel.swift` should show `1.5` not `0.5` in the generateThumbnail method.
  </verify>
  <done>
RecordingViewModel.generateThumbnail() extracts the thumbnail frame at 1.5 seconds into the clip instead of 0.5 seconds, producing a more natural-looking thumbnail where the person is composed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify audio session configuration for silent mode (QUAL-03)</name>
  <files>
    TOY/TOYApp.swift
  </files>
  <action>
Read TOYApp.swift and verify the existing audio session configuration in `init()`:
1. Confirm `AVAudioSession.sharedInstance().setCategory(.playback, mode: .moviePlayback)` is called
2. Confirm `AVAudioSession.sharedInstance().setActive(true)` is called
3. Confirm this runs in `TOYApp.init()` (before any video playback occurs)
4. Grep the entire codebase for other `AVAudioSession` usage to confirm no other code resets the category to `.ambient` or `.soloAmbient` (which would re-enable silent switch behavior)

Run: `grep -rn "AVAudioSession" TOY/ --include="*.swift"` to check for any conflicting audio session configuration.

The `.playback` category correctly ignores the silent switch. The `.moviePlayback` mode is optimized for video playback. This is already correctly implemented.

If the audio session configuration is present and no conflicting code exists, this task is COMPLETE with no code changes needed. Document the verification in the summary.

Do NOT add `.mixWithOthers` option. Do NOT change to `.ambient`. Do NOT move audio session config to individual views.
  </action>
  <verify>
Run `grep -rn "AVAudioSession" TOY/ --include="*.swift"` and confirm:
1. TOYApp.swift contains `.setCategory(.playback, mode: .moviePlayback)`
2. TOYApp.swift contains `.setActive(true)`
3. No other file sets a conflicting audio session category
  </verify>
  <done>
Audio session is confirmed configured with `.playback` category in TOYApp.init(), which correctly overrides the device silent switch. No conflicting audio session configuration exists elsewhere in the codebase.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds with no errors
2. RecordingViewModel.swift uses CMTime(seconds: 1.5, ...) for thumbnail generation
3. TOYApp.swift contains correct audio session configuration (.playback category)
4. No conflicting AVAudioSession usage exists in the codebase
</verification>

<success_criteria>
- Thumbnail generation uses 1.5s time constant instead of 0.5s
- Audio session configuration verified as correct for silent mode override
- No conflicting audio session code anywhere in the project
- Project builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-quick-playback-wins/09-02-SUMMARY.md`
</output>

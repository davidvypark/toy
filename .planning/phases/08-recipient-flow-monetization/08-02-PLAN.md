---
phase: 08-recipient-flow-monetization
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - sendtoycard-web/app/watch/[token]/page.tsx
  - sendtoycard-web/components/VideoPlayer.tsx
  - sendtoycard-web/components/ShareButtons.tsx
  - sendtoycard-web/app/not-found.tsx
autonomous: true

must_haves:
  truths:
    - "Recipient can view video at /watch/{shareToken}"
    - "Video auto-plays (muted) when page loads"
    - "TOY branding appears subtly on video player"
    - "Recipient can share video URL to social media"
  artifacts:
    - path: "sendtoycard-web/app/watch/[token]/page.tsx"
      provides: "Video viewer page with server-side data fetching"
      min_lines: 50
    - path: "sendtoycard-web/components/VideoPlayer.tsx"
      provides: "HTML5 video player with branding and unmute button"
      exports: ["VideoPlayer"]
    - path: "sendtoycard-web/components/ShareButtons.tsx"
      provides: "Social share buttons for URL sharing"
      exports: ["ShareButtons"]
  key_links:
    - from: "sendtoycard-web/app/watch/[token]/page.tsx"
      to: "Supabase cards table"
      via: "createServerClient() query"
      pattern: "from\\(['\"]cards['\"]\\)"
    - from: "sendtoycard-web/app/watch/[token]/page.tsx"
      to: "Supabase storage"
      via: "createSignedUrl"
      pattern: "createSignedUrl"
---

<objective>
Build the recipient video viewer page at /watch/{shareToken} with auto-play, TOY branding, and social sharing.

Purpose: This is the core recipient experience - when someone receives a TOY card link, they open this page to watch their personalized video greeting.

Output: A complete video viewing experience with auto-play, branding overlay, and share buttons for social media.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-recipient-flow-monetization/08-RESEARCH.md
@.planning/phases/08-recipient-flow-monetization/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create video viewer page with server-side data fetching</name>
  <files>
    sendtoycard-web/app/watch/[token]/page.tsx
    sendtoycard-web/app/not-found.tsx
  </files>
  <action>
    Create the watch page at sendtoycard-web/app/watch/[token]/page.tsx:

    Server Component that:
    1. Extracts shareToken from params
    2. Uses createServerClient() to query cards table:
       - SELECT * FROM cards WHERE share_token = {token} AND status = 'published'
    3. If card not found or not published, return notFound()
    4. Generate signed URL for card.video_url from videos bucket (1 hour expiry)
    5. Pass data to client components

    Set dynamic metadata for social sharing:
    - title: "A video for {recipientName} | TOY"
    - description: "{title}"
    - openGraph image (can be placeholder for now)

    Create custom not-found.tsx with TOY branding:
    - "Video not found" message
    - "This video may not exist or hasn't been published yet"
    - Link to main TOY website (placeholder)

    IMPORTANT: Use async/await in server component for data fetching.
    Handle errors gracefully - show not-found for missing/unpublished cards.
  </action>
  <verify>
    - Page compiles without TypeScript errors
    - Visiting /watch/invalid-token shows not-found page
    - Server component properly fetches card data
  </verify>
  <done>
    Video viewer page fetches card data and generates signed video URL on server.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create VideoPlayer component with branding and auto-play</name>
  <files>
    sendtoycard-web/components/VideoPlayer.tsx
  </files>
  <action>
    Create client component sendtoycard-web/components/VideoPlayer.tsx:

    'use client' directive required for interactivity.

    Props:
    - videoUrl: string (signed URL)
    - recipientName: string

    Features:
    1. HTML5 video element with attributes for mobile Safari:
       - autoPlay, muted, playsInline, loop
       - controls={false} (we provide custom unmute button)
    2. Subtle TOY branding overlay (bottom right):
       - "Made with TOY" text
       - Semi-transparent, small, doesn't obstruct video
       - Use Tailwind: text-white text-sm opacity-60 drop-shadow
    3. Unmute button (bottom left):
       - Speaker icon that toggles muted state
       - onClick toggles video.muted
       - Visual feedback (different icon for muted/unmuted)
    4. Responsive sizing:
       - max-w-2xl mx-auto for desktop constraint
       - aspect-video for proper ratio
       - rounded-lg shadow-xl for polish

    Use useRef for video element reference.
    Use useState for isMuted state.

    Handle video load errors gracefully with error state.
  </action>
  <verify>
    - Component renders without errors
    - Video element has autoPlay muted playsInline attributes
    - Unmute button toggles audio
    - TOY branding visible but subtle
  </verify>
  <done>
    VideoPlayer component auto-plays muted video with branding and unmute control.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ShareButtons component for social sharing</name>
  <files>
    sendtoycard-web/components/ShareButtons.tsx
  </files>
  <action>
    Create client component sendtoycard-web/components/ShareButtons.tsx:

    'use client' directive for Web Share API access.

    Props:
    - shareUrl: string (the watch URL)
    - title: string (card title)
    - recipientName: string

    Features:
    1. Primary share button using Web Share API (if available):
       - navigator.share({ url, title, text })
       - Falls back to copy-to-clipboard if Web Share not supported
    2. Direct platform buttons (optional, for fallback):
       - Twitter/X: https://twitter.com/intent/tweet?url={url}&text={text}
       - Facebook: https://www.facebook.com/sharer/sharer.php?u={url}
       - WhatsApp: https://wa.me/?text={url}
    3. Copy link button:
       - navigator.clipboard.writeText(shareUrl)
       - Show "Copied!" feedback briefly

    Styling:
    - Horizontal button group
    - TOY brand colors for primary action
    - Icon + text labels
    - Responsive layout (stack on mobile if needed)

    Use useState for copy feedback state.
    Check for navigator.share support before showing native share button.
  </action>
  <verify>
    - Component renders share buttons
    - Copy button copies URL to clipboard
    - Platform share links open in new tab
    - Web Share API used when available
  </verify>
  <done>
    ShareButtons component enables recipients to share video URL via multiple channels.
  </done>
</task>

</tasks>

<verification>
- `cd sendtoycard-web && npm run build` passes
- /watch/[token] page renders VideoPlayer and ShareButtons
- Video has autoPlay muted playsInline attributes
- TOY branding visible on video player
- Share buttons functional (copy, platform links)
- Invalid tokens show not-found page
</verification>

<success_criteria>
- Recipient can open /watch/{shareToken} and see their video
- Video auto-plays muted with option to unmute
- Subtle "Made with TOY" branding on video
- Share buttons allow URL sharing to social platforms
- Graceful handling of invalid/unpublished cards
</success_criteria>

<output>
After completion, create `.planning/phases/08-recipient-flow-monetization/08-02-SUMMARY.md`
</output>

---
phase: 08-recipient-flow-monetization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sendtoycard-web/package.json
  - sendtoycard-web/lib/supabase.ts
  - sendtoycard-web/app/layout.tsx
  - sendtoycard-web/tailwind.config.ts
autonomous: true
user_setup:
  - service: vercel
    why: "Deploy recipient web viewer"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: SUPABASE_SERVICE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (secret)"
    dashboard_config:
      - task: "Connect GitHub repo to Vercel project"
        location: "Vercel Dashboard -> New Project -> Import"

must_haves:
  truths:
    - "Next.js project exists at sendtoycard-web/"
    - "Supabase client can connect to production database"
    - "Project is deployable to Vercel"
  artifacts:
    - path: "sendtoycard-web/package.json"
      provides: "Next.js project dependencies"
      contains: "next"
    - path: "sendtoycard-web/lib/supabase.ts"
      provides: "Server-side Supabase client"
      exports: ["createServerClient"]
    - path: "sendtoycard-web/app/layout.tsx"
      provides: "Root layout with TOY branding colors"
  key_links:
    - from: "sendtoycard-web/lib/supabase.ts"
      to: "Supabase"
      via: "createClient with env vars"
      pattern: "process\\.env\\.SUPABASE"
---

<objective>
Create the Next.js web project for the recipient video viewer at sendtoycard.com.

Purpose: Recipients need a web-based way to view their video cards without installing the iOS app. This is the foundation for the /watch/{token} page.

Output: A deployable Next.js project with Supabase integration and TOY brand styling ready for the video viewer page.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-recipient-flow-monetization/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project with dependencies</name>
  <files>
    sendtoycard-web/package.json
    sendtoycard-web/tsconfig.json
    sendtoycard-web/tailwind.config.ts
    sendtoycard-web/postcss.config.mjs
    sendtoycard-web/next.config.ts
    sendtoycard-web/.env.local.example
  </files>
  <action>
    Create Next.js 14+ project in sendtoycard-web/ directory at project root (NOT inside TOY/).

    Use `npx create-next-app@latest sendtoycard-web --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*"`

    Then install Supabase: `cd sendtoycard-web && npm install @supabase/supabase-js`

    Create .env.local.example with placeholder env vars:
    - SUPABASE_URL=your_supabase_url
    - SUPABASE_SERVICE_KEY=your_service_role_key (for server-side signed URL generation)

    Update tailwind.config.ts to include TOY brand colors:
    - toyPrimary: '#E8735A' (coral)
    - toyBackground: '#FFFBF5' (warm cream)
    - toySurface: '#FFFFFF'
    - toyText: '#2D2A26' (warm black)

    Add DM Serif Display font via next/font/google.
  </action>
  <verify>
    - `cd sendtoycard-web && npm run build` succeeds
    - package.json contains @supabase/supabase-js dependency
    - tailwind.config.ts contains toyPrimary color
  </verify>
  <done>
    Next.js project exists, builds successfully, has Supabase dependency and TOY brand colors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Supabase client and root layout</name>
  <files>
    sendtoycard-web/lib/supabase.ts
    sendtoycard-web/app/layout.tsx
    sendtoycard-web/app/page.tsx
    sendtoycard-web/app/globals.css
  </files>
  <action>
    Create sendtoycard-web/lib/supabase.ts with server-side Supabase client:

    ```typescript
    import { createClient } from '@supabase/supabase-js'

    // Server-side client with service role for signed URL generation
    export function createServerClient() {
      const supabaseUrl = process.env.SUPABASE_URL
      const supabaseServiceKey = process.env.SUPABASE_SERVICE_KEY

      if (!supabaseUrl || !supabaseServiceKey) {
        throw new Error('Missing Supabase environment variables')
      }

      return createClient(supabaseUrl, supabaseServiceKey)
    }
    ```

    Update app/layout.tsx:
    - Set metadata title: "TOY - Thinking Of You"
    - Set metadata description for social sharing
    - Add DM Serif Display font from Google Fonts
    - Set body background to toyBackground color

    Update app/page.tsx to simple landing (will be replaced later):
    - "TOY - Video greeting cards" heading
    - Brief tagline

    Update globals.css with base styles using TOY colors.
  </action>
  <verify>
    - `npm run dev` starts successfully
    - Visiting localhost:3000 shows TOY branding
    - No TypeScript errors on build
  </verify>
  <done>
    Root layout has TOY branding, Supabase client is configured for server-side use.
  </done>
</task>

</tasks>

<verification>
- `cd sendtoycard-web && npm run build` passes without errors
- `npm run lint` has no critical issues
- lib/supabase.ts exports createServerClient function
- tailwind.config.ts includes TOY brand colors
- .env.local.example documents required environment variables
</verification>

<success_criteria>
- Next.js project builds and runs locally
- Supabase client configured for server-side usage
- TOY brand colors and typography applied
- Project structure ready for /watch/[token] page
</success_criteria>

<output>
After completion, create `.planning/phases/08-recipient-flow-monetization/08-01-SUMMARY.md`
</output>

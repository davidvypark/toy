---
phase: 08-recipient-flow-monetization
plan: 06
type: execute
wave: 3
depends_on: ["08-02", "08-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Recipient can view video at sendtoycard.com/watch/{token}"
    - "Video auto-plays when page loads"
    - "TOY branding visible on video player"
    - "Share buttons work on mobile and desktop"
    - "Upgrade flow works in iOS app"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete recipient flow and monetization integration.

Purpose: Verify the entire end-to-end experience works correctly across web viewer and iOS upgrade flow before Phase 8 completion.

Output: Confirmed working recipient viewing experience and monetization flow, or documented issues for gap closure.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-recipient-flow-monetization/08-02-SUMMARY.md
@.planning/phases/08-recipient-flow-monetization/08-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy web viewer to Vercel</name>
  <action>
    Deploy the sendtoycard-web project to Vercel:

    1. Create Vercel account if needed (vercel.com)
    2. Install Vercel CLI: `npm i -g vercel`
    3. From sendtoycard-web directory: `vercel`
    4. Set environment variables in Vercel dashboard:
       - SUPABASE_URL
       - SUPABASE_SERVICE_KEY
    5. Configure custom domain sendtoycard.com (if ready) or use Vercel subdomain for testing

    Claude will assist with CLI commands, but Vercel account setup requires human action.
  </action>
  <instructions>
    1. If you don't have a Vercel account, create one at vercel.com
    2. Run `cd sendtoycard-web && vercel` to deploy
    3. Add environment variables in Vercel dashboard
    4. Note the deployment URL (e.g., sendtoycard-web.vercel.app)
    5. Type the deployment URL when ready to continue
  </instructions>
  <resume-signal>Provide the Vercel deployment URL</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete recipient video viewing experience:
    - Web video player at /watch/{shareToken}
    - Auto-play with muted video
    - TOY branding overlay
    - Share buttons for social media
  </what-built>
  <how-to-verify>
    Prerequisites: Have a published card with a shareToken.

    Web Viewer Testing:
    1. Open {deployment-url}/watch/{shareToken} in desktop browser
       - Video should auto-play (muted)
       - "Made with TOY" branding visible (bottom right)
       - Unmute button works
       - Share buttons appear

    2. Open same URL on iPhone Safari
       - Video auto-plays (muted, inline - no fullscreen takeover)
       - Unmute button toggles audio
       - Copy link button works
       - Platform share links work

    3. Open same URL on Android Chrome
       - Video plays (may need tap on some devices)
       - Share functionality works

    4. Test invalid token
       - Visit /watch/invalid-token-12345
       - Should show "Video not found" page

    5. Test unpublished card (if you have one)
       - Should show "Video not found" (not published yet)
  </how-to-verify>
  <resume-signal>Type "web-approved" if all web tests pass, or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    iOS monetization flow:
    - Upgrade prompt when card reaches 8 participants
    - RevenueCat purchase flow
    - Free tier works for small cards
  </what-built>
  <how-to-verify>
    Prerequisites:
    - RevenueCat configured with test product
    - Sandbox tester account in App Store Connect

    Free Tier Testing:
    1. Create a new card with < 8 participants
       - NO upgrade prompt should appear
       - All features work normally
       - Can generate invite links

    Upgrade Flow Testing:
    2. Create a card and add 8 participants (or mock the count)
       - Upgrade banner should appear in CardDetailView
       - "Card is full - Upgrade for more participants" message

    3. Tap upgrade banner
       - CardUpgradeView sheet opens
       - Shows current participant count
       - Shows benefits (unlimited participants, etc.)
       - Shows price from RevenueCat

    4. Test purchase (with sandbox account)
       - Tap "Upgrade - $X.XX" button
       - App Store payment sheet appears
       - Complete purchase with sandbox credentials
       - Success state shows
       - Sheet dismisses
       - Card now allows unlimited participants

    5. Test restore purchases
       - Sign in with same sandbox account on fresh install
       - Tap "Restore Purchases"
       - Previous purchase should restore

    Note: If RevenueCat products aren't configured yet, note what's missing.
  </how-to-verify>
  <resume-signal>Type "ios-approved" if all iOS tests pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Web viewer deployed and accessible
- Video auto-plays on mobile and desktop
- TOY branding visible on video
- Share buttons functional
- Upgrade prompt appears at free tier limit
- Purchase flow completes successfully
- Free tier cards work without prompts
</verification>

<success_criteria>
- All Phase 8 success criteria verified:
  1. Recipient can open link and view the final montage - VERIFIED
  2. Video auto-plays when recipient opens the link - VERIFIED
  3. Video displays with subtle TOY branding - VERIFIED
  4. Recipient can reshare video to social media - VERIFIED
  5. Host can purchase per-card upgrades via RevenueCat - VERIFIED
  6. Free tier works for cards with 8 or fewer participants - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/08-recipient-flow-monetization/08-06-SUMMARY.md`
</output>

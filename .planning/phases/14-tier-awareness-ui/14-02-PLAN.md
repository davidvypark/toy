---
phase: 14-tier-awareness-ui
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - TOY/Features/Monetization/TierSelectionSheet.swift
  - TOY/Features/CardManagement/CardDetailView.swift
autonomous: true

must_haves:
  truths:
    - "Host can tap the tier indicator to open a tier selection sheet"
    - "TierSelectionSheet displays all 4 tiers (Free, Starter, Group, Mega) with clip limits"
    - "TierSelectionSheet shows real prices from RevenueCat for paid tiers"
    - "The required tier for the current clip count is visually highlighted"
    - "TierSelectionSheet has NO purchase button -- browse only (Phase 15 adds purchase)"
    - "If price fetching fails, the sheet still shows tier names and clip limits gracefully"
  artifacts:
    - path: "TOY/Features/Monetization/TierSelectionSheet.swift"
      provides: "Tier browsing sheet with price display"
      contains: "struct TierSelectionSheet"
    - path: "TOY/Features/CardManagement/CardDetailView.swift"
      provides: "Sheet presentation wired to showTierSelection state"
      contains: ".sheet.*showTierSelection"
  key_links:
    - from: "CardDetailView.swift"
      to: "TierSelectionSheet.swift"
      via: ".sheet(isPresented: $showTierSelection)"
      pattern: "TierSelectionSheet\\("
    - from: "TierSelectionSheet.swift"
      to: "PurchaseService.swift"
      via: "fetchTierPackages() for price display"
      pattern: "PurchaseService\\.shared\\.fetchTierPackages"
    - from: "TierSelectionSheet.swift"
      to: "CardTier.swift"
      via: "CardTier.allCases for tier iteration, CardTier.requiredTier for highlighting"
      pattern: "CardTier\\.(allCases|requiredTier|fromMaxParticipants)"
---

<objective>
Create the TierSelectionSheet for browsing available tiers with real pricing, and wire it to the tier indicator's tap action in CardDetailView.

Purpose: Hosts can explore the tier system and see what each tier costs before they need to publish. This is a browse-only view -- Phase 15 will add the purchase button.

Output: TierSelectionSheet.swift with tier rows and price fetching, CardDetailView updated with sheet presentation.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tier-awareness-ui/14-RESEARCH.md
@.planning/phases/14-tier-awareness-ui/14-01-SUMMARY.md
@TOY/TOYShared/Sources/TOYShared/Models/CardTier.swift
@TOY/Features/Monetization/PurchaseService.swift
@TOY/Features/CardManagement/CardDetailView.swift
@TOY/Features/Monetization/CardUpgradeView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TierSelectionSheet with tier rows and price fetching</name>
  <files>
    TOY/Features/Monetization/TierSelectionSheet.swift
  </files>
  <action>
Create `TOY/Features/Monetization/TierSelectionSheet.swift`. This is a sheet view that displays all tiers with real prices from RevenueCat.

**Structure:**

```swift
import SwiftUI
import RevenueCat
import TOYShared
```

**TierSelectionSheet** -- Main view:
- Properties: `let card: Card`, `let clipCount: Int`
- State: `@State private var packages: [String: Package] = [:]`, `@State private var isLoading = true`, `@State private var errorMessage: String?`
- Environment: `@Environment(\.dismiss) private var dismiss`
- Computed: `private var requiredTier: CardTier { CardTier.requiredTier(for: clipCount) }`, `private var currentTier: CardTier { CardTier.fromMaxParticipants(card.maxParticipants) }`

Body layout:
- `NavigationStack` wrapping `ZStack` with `TOYBackground()` + `ScrollView`
- Header section: "Card Tiers" title (`.toyTitle()`, `.toyText`), subtitle "{clipCount} clips on this card" (`.toySubheadline()`, `.toyTextSecondary`)
- Tier list: Iterate `CardTier.allCases` showing a `TierRowView` for each tier
- Loading state: Show `ProgressView()` centered while prices load
- Error state: If `errorMessage` is set, show tier rows without prices (graceful degradation) -- tier names and clip limits always display
- `.task { await loadPrices() }` to fetch prices on appear
- Toolbar: "Done" button (`.navigationBarLeading`) to dismiss, styled `.toyBody()` / `.toyTextSecondary`
- `.navigationBarTitleDisplayMode(.inline)`

Price helper:
```swift
private func priceString(for tier: CardTier) -> String? {
    guard let identifier = tier.packageIdentifier,
          let package = packages[identifier] else {
        return tier == .free ? "Free" : nil
    }
    return package.localizedPriceString
}
```

Price loading:
```swift
private func loadPrices() async {
    do {
        packages = try await PurchaseService.shared.fetchTierPackages()
    } catch {
        errorMessage = "Unable to load prices"
    }
    isLoading = false
}
```

**TierRowView** -- Private inner view for each tier row:
- Properties: `let tier: CardTier`, `let priceString: String?`, `let isRequired: Bool`, `let isCurrentTier: Bool`, `let clipCount: Int`
- Layout: HStack with left VStack (tier name + clip limit) + Spacer + right VStack (price + status label)
  - Tier name: `tier.displayName` in `.toyBodyMedium()`, `.toyText`
  - Clip limit: "Up to {tier.clipLimit} clips" for finite tiers, "Unlimited clips" for mega, in `.toyCaption()`, `.toyTextSecondary`
  - Price: `priceString ?? "—"` in `.toyBodyMedium()`, `.toyText` (show dash if price unavailable)
  - Status label (below price): "Required" if `isRequired && !isCurrentTier`, "Current" if `isCurrentTier`, empty otherwise, in `.toyCaption()`, `.toyTextSecondary`
- Padding: `TOYSpacing.md`
- Border: `.toyText` 1px stroke if `isRequired` (bold highlight for the tier the user needs), `.toyDivider` 1px stroke for all others
- No tap action -- browse only in Phase 14

**CRITICAL SCOPE BOUNDARY:** NO purchase button, NO purchase() calls, NO "Buy" CTA anywhere. Phase 15 adds purchase. The sheet layout should have natural space at the bottom where Phase 15 will add a purchase CTA, but leave it empty.

**Design consistency:** Follow existing TOY monochrome design system:
- TOYBackground() for sheet background
- `.toyText`, `.toyTextSecondary`, `.toyDivider` for colors
- `.toyTitle()`, `.toyBodyMedium()`, `.toyBody()`, `.toyCaption()`, `.toySubheadline()` for typography
- `TOYSpacing.xs`, `.sm`, `.md`, `.lg`, `.xl` for spacing
- Horizontal padding: `TOYSpacing.lg` (24px), matching CardDetailView
  </action>
  <verify>
Build the project: `xcodebuild -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`. Verify the file compiles. Grep the file to confirm: NO occurrence of `purchase(`, NO occurrence of "Buy" or "Purchase" button text.
  </verify>
  <done>
TierSelectionSheet.swift exists with: all 4 tiers displayed, real prices via PurchaseService.fetchTierPackages(), required tier highlighted with bold border, graceful error handling when prices fail to load, and NO purchase button or purchase logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire TierSelectionSheet to CardDetailView</name>
  <files>
    TOY/Features/CardManagement/CardDetailView.swift
  </files>
  <action>
Update CardDetailView.swift to present TierSelectionSheet when the tier indicator is tapped:

1. **Add `.sheet` modifier** for TierSelectionSheet. Add it near the existing `.sheet(isPresented: $showUpgradeSheet)` modifier:

```swift
.sheet(isPresented: $showTierSelection) {
    TierSelectionSheet(card: card, clipCount: viewModel.clips.count)
}
```

2. **Remove the placeholder comment** from Plan 01 (if it was added: `// TierSelectionSheet will be wired here in Plan 02`).

3. **Verify the connection:** The `showTierSelection` state variable (added in Plan 01) is set to `true` in the TierIndicatorView's `onTapUpgrade` closure, and the `.sheet` modifier presents TierSelectionSheet when this state is true. This completes the tap flow: indicator tap -> showTierSelection = true -> sheet presents -> user browses tiers -> taps Done -> sheet dismisses.

No other changes needed. The `showUpgradeSheet` / `CardUpgradeView` sheet remains untouched (Phase 16 cleanup).
  </action>
  <verify>
Build the project: `xcodebuild -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`. Verify: CardDetailView has `.sheet(isPresented: $showTierSelection)` presenting `TierSelectionSheet`. Verify: `showTierSelection` is toggled by `TierIndicatorView`'s `onTapUpgrade` closure.
  </verify>
  <done>
Tapping the tier indicator opens the TierSelectionSheet. The full flow works: clips > maxParticipants -> tier indicator visible -> tap -> TierSelectionSheet opens -> browse tiers with prices -> tap Done -> dismiss. No purchase action available (correct for Phase 14).
  </done>
</task>

</tasks>

<verification>
1. Build succeeds with no errors or warnings related to tier selection
2. TierSelectionSheet shows all 4 tiers from CardTier.allCases
3. Prices come from PurchaseService.fetchTierPackages() (localizedPriceString)
4. Required tier row has a visually distinct border (`.toyText` instead of `.toyDivider`)
5. No purchase button or purchase logic exists in TierSelectionSheet
6. Sheet dismisses via Done button
7. CardDetailView presents TierSelectionSheet via `.sheet(isPresented: $showTierSelection)`
</verification>

<success_criteria>
- Host can tap tier indicator to open TierSelectionSheet
- All 4 tiers displayed with names, clip limits, and real prices
- Required tier is visually highlighted
- No purchase button exists (browse-only, Phase 15 adds purchase)
- Price fetch failure degrades gracefully (tier names and limits still show)
</success_criteria>

<output>
After completion, create `.planning/phases/14-tier-awareness-ui/14-02-SUMMARY.md`
</output>

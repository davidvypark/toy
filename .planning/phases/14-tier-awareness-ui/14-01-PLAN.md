---
phase: 14-tier-awareness-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TOY/Features/CardManagement/CardDetailViewModel.swift
  - TOY/Features/Monetization/TierIndicatorView.swift
  - TOY/Features/CardManagement/CardDetailView.swift
autonomous: true

must_haves:
  truths:
    - "Card detail view shows a tier indicator with clip count and tier status when clips exceed card.maxParticipants"
    - "When a card is within its free allowance (clips <= card.maxParticipants), no tier indicator appears"
    - "The old 'Card is full' upgrade banner is replaced by the new tier indicator"
    - "Grandfathered cards (maxParticipants=8) show no tier info until clip 9"
  artifacts:
    - path: "TOY/Features/CardManagement/CardDetailViewModel.swift"
      provides: "Tier computed properties: clipCount, requiredTier(for:), purchasedTier(for:), needsUpgradeToPublish(for:)"
      contains: "CardTier.requiredTier"
    - path: "TOY/Features/Monetization/TierIndicatorView.swift"
      provides: "Stateless tier indicator component"
      contains: "struct TierIndicatorView"
    - path: "TOY/Features/CardManagement/CardDetailView.swift"
      provides: "Tier indicator wired in place of old upgrade banner"
      contains: "TierIndicatorView"
  key_links:
    - from: "CardDetailView.swift"
      to: "TierIndicatorView.swift"
      via: "TierIndicatorView instantiation with viewModel computed properties"
      pattern: "TierIndicatorView\\("
    - from: "CardDetailViewModel.swift"
      to: "CardTier.swift"
      via: "CardTier.requiredTier and CardTier.fromMaxParticipants calls"
      pattern: "CardTier\\.(requiredTier|fromMaxParticipants)"
    - from: "CardDetailView.swift"
      to: "card.maxParticipants"
      via: "Visibility threshold uses card.maxParticipants (not CardTier.free.clipLimit)"
      pattern: "viewModel\\.clips\\.count > card\\.maxParticipants"
---

<objective>
Add tier awareness to the card detail screen: computed properties on the ViewModel for tier status, a new TierIndicatorView component, and wire it into CardDetailView replacing the old binary "Card is full" upgrade banner.

Purpose: Hosts see where their card stands in the tier system -- clip count, required tier, and a tappable indicator to explore tiers. This is the core display layer that Phase 14's TierSelectionSheet (Plan 02) will connect to.

Output: CardDetailViewModel with tier computed properties, TierIndicatorView.swift, updated CardDetailView with tier indicator replacing upgrade banner.
</objective>

<execution_context>
@/Users/davidpark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/davidpark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-tier-awareness-ui/14-RESEARCH.md
@TOY/TOYShared/Sources/TOYShared/Models/CardTier.swift
@TOY/Features/CardManagement/CardDetailView.swift
@TOY/Features/CardManagement/CardDetailViewModel.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier computed properties to CardDetailViewModel and create TierIndicatorView</name>
  <files>
    TOY/Features/CardManagement/CardDetailViewModel.swift
    TOY/Features/Monetization/TierIndicatorView.swift
  </files>
  <action>
**CardDetailViewModel.swift** -- Add 4 computed properties/methods after the existing `effectiveDuration(for:)` method, in a new `// MARK: - Tier Status` section:

1. `var clipCount: Int { clips.count }` -- convenience for views
2. `func requiredTier(for card: Card) -> CardTier` -- returns `CardTier.requiredTier(for: clips.count)`
3. `func purchasedTier(for card: Card) -> CardTier` -- returns `CardTier.fromMaxParticipants(card.maxParticipants)`
4. `func needsUpgradeToPublish(for card: Card) -> Bool` -- returns `requiredTier(for: card) > purchasedTier(for: card)`

These are functions (not computed properties) because they need the `Card` parameter. The ViewModel does NOT store the card -- it's passed by CardDetailView.

**TierIndicatorView.swift** -- Create new file at `TOY/Features/Monetization/TierIndicatorView.swift`. This is a pure stateless SwiftUI view:

```swift
import SwiftUI
import TOYShared

struct TierIndicatorView: View {
    let clipCount: Int
    let requiredTier: CardTier
    let purchasedTier: CardTier
    let onTapUpgrade: () -> Void
```

Design:
- Button wrapping an HStack with left-aligned VStack (two lines of text) + Spacer + chevron.right icon
- Line 1: "{clipCount} clips submitted" in `.toyBodyMedium()`, `.toyText` color
- Line 2: Tier status text -- if requiredTier == purchasedTier: "{tier.displayName} tier", else: "{requiredTier.displayName} tier to publish" in `.toyCaption()`, `.toyTextSecondary` color
- Chevron: system image "chevron.right", size 14 medium weight, `.toyTextSecondary` color
- Padding: `TOYSpacing.md` inside
- Border: `Rectangle().stroke(Color.toyDivider, lineWidth: 1)` -- subtle informational style matching inviteLinkView
- `.buttonStyle(.plain)`
- NO network calls, NO state, NO ViewModel -- purely receives data as parameters

This matches the existing inviteLinkView / upgradeBannerView visual pattern (outlined border, not solid fill).
  </action>
  <verify>
Build the project: `xcodebuild -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`. Both files should compile without errors. CardTier should be accessible from CardDetailViewModel via TOYShared import (already imported).
  </verify>
  <done>
CardDetailViewModel has 4 tier-related methods using CardTier enum. TierIndicatorView.swift exists as a stateless component with clip count, tier status text, and tappable chevron. Both compile cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire TierIndicatorView into CardDetailView, replacing the old upgrade banner</name>
  <files>
    TOY/Features/CardManagement/CardDetailView.swift
  </files>
  <action>
Modify CardDetailView.swift to replace the old binary upgrade banner with the new tier-aware indicator:

1. **Add state variable:** Add `@State private var showTierSelection = false` alongside existing `@State` properties (near `showUpgradeSheet`).

2. **Replace the upgrade banner block** in `body`: Find the block:
```swift
// Upgrade banner
if needsUpgrade {
    upgradeBannerView
}
```
Replace with:
```swift
// Tier indicator (only shown when clips exceed card's free allowance)
if viewModel.clips.count > card.maxParticipants {
    TierIndicatorView(
        clipCount: viewModel.clips.count,
        requiredTier: viewModel.requiredTier(for: card),
        purchasedTier: viewModel.purchasedTier(for: card),
        onTapUpgrade: { showTierSelection = true }
    )
}
```

**CRITICAL:** The visibility check uses `viewModel.clips.count > card.maxParticipants`, NOT `> CardTier.free.clipLimit`. This handles grandfathered cards (maxParticipants=8) correctly -- they see no indicator until clip 9.

3. **Remove the old `needsUpgrade` computed property** (lines ~98-100): Delete the `private var needsUpgrade: Bool` computed property entirely. It uses `participants.count >= card.maxParticipants` which is the old binary logic.

4. **Remove the old `upgradeBannerView`** (lines ~435-462): Delete the entire `private var upgradeBannerView: some View` property. The TierIndicatorView fully replaces it.

5. **Keep the old `showUpgradeSheet` state and `.sheet(isPresented: $showUpgradeSheet)` for CardUpgradeView** -- do NOT remove these yet. They are unused after this change (nothing sets `showUpgradeSheet = true` anymore), but Phase 16 will clean them up. The new `showTierSelection` state will be wired to the TierSelectionSheet in Plan 02.

6. **Add the TierSelectionSheet presentation** as a new `.sheet` modifier. For now, since TierSelectionSheet is created in Plan 02, add a placeholder comment where the sheet will go:
```swift
// TierSelectionSheet will be wired here in Plan 02
```
This will be completed in 14-02.

Do NOT add any import statements -- SwiftUI and TOYShared are already imported.
  </action>
  <verify>
Build the project: `xcodebuild -scheme TOY -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -20`. Verify: no references to `needsUpgrade` or `upgradeBannerView` remain in CardDetailView. Verify: `TierIndicatorView` appears in CardDetailView body. Verify: visibility condition uses `card.maxParticipants` not `CardTier.free.clipLimit`.
  </verify>
  <done>
CardDetailView shows TierIndicatorView when clips exceed card.maxParticipants. The old needsUpgrade computed property and upgradeBannerView are removed. Grandfathered cards (maxParticipants=8) show no indicator for 8 or fewer clips. The indicator's tap action sets showTierSelection = true (to be wired to TierSelectionSheet in Plan 02).
  </done>
</task>

</tasks>

<verification>
1. Build succeeds with no errors or warnings related to tier indicator
2. `needsUpgrade` and `upgradeBannerView` are fully removed from CardDetailView
3. TierIndicatorView is a stateless view with no network calls or ViewModel
4. Visibility threshold uses `card.maxParticipants` (not `CardTier.free.clipLimit`)
5. CardDetailViewModel tier methods use `CardTier.requiredTier(for:)` and `CardTier.fromMaxParticipants(_:)` -- no hand-rolled tier logic
</verification>

<success_criteria>
- CardDetailView shows TierIndicatorView with clip count and tier status when clips > card.maxParticipants
- CardDetailView shows NO tier information when clips <= card.maxParticipants
- Old upgrade banner is fully replaced (needsUpgrade and upgradeBannerView deleted)
- Grandfathered cards (maxParticipants=8) are handled correctly
</success_criteria>

<output>
After completion, create `.planning/phases/14-tier-awareness-ui/14-01-SUMMARY.md`
</output>
